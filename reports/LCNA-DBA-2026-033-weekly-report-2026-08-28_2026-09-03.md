# DBA / 基础架构 工作周报

**文档编号**：LCNA-DBA-2026-033
**报告周期**：2026-08-28（周五） ~ 2026-09-03（周四）
**撰写人**：曾翔宇（David Zeng）· Senior DBA / Infrastructure Engineer
**生成时间**：2026-09-03

> **口径说明**：本报告基于各代码仓库在报告周期内的提交记录、产物文件，以及 **2026-09-03 的现场实测**整理。
> 本期有实质提交的仓库六个：`luckin-usa-dba-ops`（主，慢 SQL + QM 数据包）、`db-alert-owner-mapping`、
> `luckin-ops-dashboard`、`luckin-store-ops-dashboard`、`luckin-efficiency-dashboard`、`alert-mailserver`。
> 第一节的告警统计、第三节的遗留闭环验证**均为当日实查**（Zeus `t_alert` / CloudWatch），非引用历史文档。
> 看板仓库另有 65（`luckin-ops-dashboard`）+ 5（`luckin-store-ops-dashboard`）次定时自动提交，非人工产出，不计入提交总数与净增代码。
> 08-24 的两份研判产物**仍未提交发布**，本期又新增一份同类未提交产物，已在第五、六节如实标注。

---

## 一、本周概览

| 维度 | 数量 |
|---|---|
| 提交总数 | **49 次实质提交**（主仓 19 / db-alert-owner-mapping 13 / luckin-ops-dashboard 9 / luckin-efficiency-dashboard 3 / luckin-store-ops-dashboard 3 / alert-mailserver 2） |
| 净增代码 | **+8,756 / −1,640 行**（不含 QM 数据包 238,704 行原始抓取数据；含数据包为 +247,460） |
| **时间分布** | **慢 SQL L0/L1 审计 + 分级定稿 + TOP10（含 3 个看板连锁修复）≈1.5 天（≈45%）** · **DB 告警 @负责人改造收尾**（Redis 配置中心接入 + batch 1 签收）≈1.3 天（≈40%） · QM 2026-08 数据包拉取 ≈0.3 天（≈9%） · alert-mailserver 深链修复 ≈0.1 天（≈3%） · 08-28 P0 快速研判 ≈0.05 天（≈2%） |
| 现网变更 | **2 项已确认生效**：db-alert-owner-mapping Redis 映射逻辑对齐国内版并重部署（08-28 20:44）+ `service_map.yaml` 4 处映射修正（存盘即生效）。**1 项待验证**：`luckin-ops-dashboard` 三处应用层修复已合并，是否已 `restart.sh` 生效尚未现场确认；**1 项待生效**：`luckin-store-ops-dashboard` 修复已合并但采集容器未重启，自 09-01 01:00 起已 2 天多无新数据推送 |
| 事故研判 | **1 起 P0**（`isalestradecapi` 服务异常数>8，08-28 13:01，判定为**整点批量尾巴、非故障、非复发**） |
| 慢 SQL 治理 | **L0+L1 共 48 台实例审计完成**；18 条候选清单 EXPLAIN 复核后收敛为 **10 条有效**（合计 47,443.4s，#1 占 48.6%）；**顺带发现并当天修复 4 处自身导致的全表扫描**（含一条 1.95 亿行/周、从未上过任何榜单的） |
| 分级体系 | **新增 65 台实例五级分级表**（L0 15 / L1 33 / L2 6 / L3 4 / L4 7），首次把安全运维/数据分析类实例移出业务档 |
| 自动化测试 | db-alert-owner-mapping **81 单测**持续通过；`luckin-store-ops-dashboard` 新增 **8 单测**（增量拼接逻辑）；`luckin-ops-dashboard` 报告面板 **新增回归测试**（防再次被折叠行坑到） |
| 上期遗留闭环 | **2 项闭环**：batch 1（#100）三条判据全绿签收、`00-工作计划` 分组@语义文档更正；**1 项无进展**：EBSByteBalance% 告警（连续三期） |

**本期形态**：与 032 的「一条主线吃六成时间」不同，本期是**两条主线并行、体量相当**——
`db-alert-owner-mapping` 从「建仓推现网」转入「补齐 Redis 归属 + 观察期签收」的收尾阶段（08-28 一整天 + 08-31 半天）；
`慢 SQL` 项目在 08-27 那份 TOP10 之后继续深挖，09-01 一天之内从 15 台 L0 扩到 48 台 L0+L1，
并且**审计过程本身连续两次揪出「榜单看不见的自己人」**——先是店铺运营看板的 2,700 万行/周扫描，
再翻出一条 4.6 倍于它、1.95 亿行/周、因为平均只有 0.62 秒而彻底躲过 ≥1s 收录门槛的效能看板查询，
两处当天定位、当天修复。

**一句话**：032 的主题是「一个不可能失败的指标，等于没有指标」。本周往前一步——
**审计者自己的查询，是审计盲区里最容易被漏掉的一类**：慢查询榜单只收 ≥1s 的指纹，
店铺运营与效能两个看板的问题查询恰好都在 1 秒门槛之下，靠常规排名永远上不了榜；
真正找到它们靠的是换一个维度——**按执行账号回溯**，而不是继续在同一张按耗时排序的榜单里深挖。
同一天，`db-alert-owner-mapping` 也在纠正一个自己的错误：08-25 把一次被 `LIMIT 300` 截断的探索查询
当成事实写进了方案文档，08-28 补跑全量后发现结论整体不变但具体数字全错，已更正并加了「不许 LIMIT 这条查询」的警告。

### 本期告警全景（Zeus `luckyus_izeus.t_alert`，2026-09-03 实查）

| 指标 | 值 |
|---|---|
| 全公司告警总量（剔除测试策略 #303） | **298 条** |
| 按级别 | P2 266 / P1 15 / P0 13 / P3 4 |
| 恢复情况 | **298 条全部 RESOLVED**（295 自动 + 3 手动），0 条未恢复 |
| 已认领 / 被自动升级 | **2 条（0.7%）** / **4 条（1.3%）** |
| 【DB告警】族（DBA 直接责任面） | **50 条**：Redis 命令时延>2ms (23)、活跃线程>30 (20)、活跃线程>45 (6)、活跃线程>60 (1) |
| batch 1（#100）实际触发 | **20 次，`domain` 全部非空（100%）**——@负责人改造上线以来首个完整周的生产验证 |
| `luckylfe-log` ES 磁盘告警 | **0 条**（连续两期为 0，第三节详解为何这不等于安全） |
| 08-28 P0（`isalestradecapi`>8） | **1 次**，13:01 UTC，与同类 P1（>2，5 次）、【全局策略】>20（6 次）同属整点批量尾巴，均自愈无人认领 |

---

## 二、工作明细

### 主线一：慢 SQL L0+L1 全量审计 + 分级定稿 + TOP10 优化清单（09-01 ~ 09-02，≈1.5 天）

承接 032 期 `slow-sql-topn-dashboard` 的台账机制，本期从「15 台 L0」扩展到「48 台 L0+L1」，
并首次产出一份**可直接外发的、只含 SQL 与建议**的版本。主仓 18 次提交，+4,215 / −633。

#### 1.1 L0 TOP3（09-01 上午）

15 台 L0 实例、7 天窗口（08-25~09-01）命中 149 个 ≥1s 慢 SQL 指纹，合计新增 DB 时间 **10,060.1 秒**，
**TOP3 占 90.7%**（TOP2 已占 87.1%）：

| 排名 | 实例 | 表 | 7d DB时间 | 占比 | 定性 |
|---|---|---|---:|---:|---|
| 1 | opshopsale | t_shopsale_spu/rmk | 4,976.9s | 49.5% | 无效巡检 + 任务重复执行 |
| 2 | salespayment | t_channel_fee | 3,780.6s | 37.6% | 业务任务空转 + **数据缺口**（单次返回恒 5,000＝分页封顶） |
| 3 | salesmarketing | t_coupon_record_expired | 371.2s | 3.7% | 缺索引 + 优化器误判 |

三条全部是后台定时任务，无一位于用户请求链路；三台实例 CPUCreditBalance 均满值、CPU 均值 5.4%~6.8%，
**当前不构成性能事故**，定性为「持续资源浪费」，但 SQL-02 同时暴露一个真实的财务侧数据完整性缺口。

#### 1.2 剩余 9 条 + 两份变更申请（09-01 下午）

L0 剩余 9 条逐条分析完，出**两份索引变更申请**（`cdpactivity` 触达记录索引、`salesorder` 财务对账索引），
台账（`reports/slow-sql-weekly/analyzed-registry.csv`）同步处置状态。**L0 全部结项，pending 归零。**

#### 1.3 按执行账号回溯：两次揪出「榜单看不见的自己人」

榜单口径（按耗时排序）之外换一个维度——按 `User@Host` 回溯 CloudWatch 慢日志原文：

- **第一次**：TOP10 里 `diagtools`（DBA 自己）已占 3 条，均为已修的 store-ops SPU 采集（2,700 万行/周，
  当天已合并双扫描并改增量）。
- **第二次、更值得记的一次**：`avgPrepTime` 每小时全表扫 `t_order_make` 145 万行，7 天合计 **1.95 亿行**，
  是刚修的 store-ops SPU 采集的 **4.6 倍**，却从未出现在任何榜单——因为它**平均只要 0.62 秒**，
  低于采集表 `≥1s` 的收录门槛，是效能看板的日更查询。当天定位、当天修复。

#### 1.4 分级体系重做：65 台实例，六级判据收敛为五级

沿用「MySQL 8.4.9 升级跟踪表」的业务重要性档位为基础，**按 David 的组件角色判据**把 11 台本身不是
业务功能的实例移出业务档，改按角色归类：

| 新档 | 台数 | 举例 |
|---|---:|---|
| L3 安全运维 | 4 | `devops`（iZeus 告警）、`ijumpserver`（堡垒机）、`iluckyhealth`（巡检） |
| L4 数据分析 | 7 | `icyberdata`、`ldas`/`ldas01`、`pubdm`、`ldasverify01/02` |

最终分布 **L0×15 / L1×33 / L2×6 / L3×4 / L4×7（共 65 台）**，65 台中 32 台档位发生变化。
标准本身当天定稿两次（六级→五级，取消 L5），并留了 3 处待确认档位（`iriskcontrolservice`、
`isalesdatamarketing`、`pubdm`）与一处判据空缺（登录/权限/日志类基础组件暂沿用跟踪表原档）。
🔴 **重跑任何从升级跟踪表生成映射的脚本只会还原 L0/L1/L2**，11 台新分类必须按 CSV 逐行 `note` 重新套用。

#### 1.5 L0+L1 TOP10 → 18 条清单 → 10 条有效（09-01 晚 ~ 09-02）

48 台 L0+L1 的 TOP10（-0901-E）与「纯清单」两个口径合并去重得到 **18 条**，
09-02 逐条跑 `EXPLAIN` 与现场实测复核后收敛：

- **剔除 5 条**（判据：现场实测耗时 ÷ 慢日志记录均值，差 10 倍以上＝慢在执行时刻、不是语句本身）
  ——含 `salesorder t_order`（87 倍差，执行计划本来就对）、`cdpactivity`（CR 已出不占名额）等。
- **合并 3 条**（同账号同模式，或同一份 CR 能一起解决）。
- **改判 2 条**：`salesorder t_order` 从「加索引」改判为「争用受害者，排查执行时刻」；
  `salesmarketing t_market_activity_partake` 索引其实**早就存在且已生效**（覆盖扫描，168ms 是数
  30 万条索引项的真实成本），上一版「建索引」的建议作废，改判计数缓存。
- **顺带查出两处队列积压**（与 SQL 优化无关但更值得处理）：`t_cyber_ug_data_fetch_task` 里 1 行任务
  卡在 `task_status=0` 已 3 个月却被轮询条件永久排除；`t_private_domain_user` 里 85 行因 `user_no`
  全为 NULL 被排除，最久近 10 个月——与 032 期 `salespayment` 空转 17 个月同类。

最终 **10 条有效，合计 47,443.4s，#1 占 48.6%、前三条占 69.0%**，逐条标出责任方
（张晓松 / 陈培浩·游熖 / 陈亮 / 张翔 / DBA 自有），09-02 出**可外发版**（只含 SQL 原文与建议，无内部研判过程）。

### 主线二：DB 告警 @负责人改造收尾（08-28、08-31，≈1.3 天）

承接 032 期新建的私有仓库 `db-alert-owner-mapping`。本期 13 次提交，+1,758 / −242，81 单测持续通过。

#### 2.1 08-25 记录的错误被 08-28 全量复核推翻

032 记录「US 无对应 Redis 归属数据源」，是搜索范围不够（只查了 `iluckyibizconfigcenter` 加两个全局可读实例）。
扫描全部 62 个网关 MySQL 服务器的 `information_schema` 后找到 `luckyus_gaea`（`framework01`）——国内查询在这里
逐字可跑。随后发现 `luckyus_gaea` 08-25 起已停止写入，真正在用的是 `luckyus_gaea_new`（33 张表，多 8 张，
当天仍有写入），配置项已切换到新库，`ldas.conf` 可覆盖。

**同一天还发现自己此前报的数字是错的**：探索查询带了 `LIMIT 300` 被截断，报成「57 个集群键、30/30 一致」；
补跑全量后是「88 个 dskey → 456 行 → 80 个集群键，剔除平台自身后 46 单值/24 多值/10 空」，
交叉核验仍是 45 个可比集群中 43 个一致——**结论方向没变，但具体数字全错**，两处已更正并标注「不许 LIMIT 这条查询」。

#### 2.2 MySQL 一半直接采用国内版逻辑，Redis 一半验证后判定不可移植

David 要求验证国内版 `get_instance_schema_exporter_internal.py` 后**采用其 MySQL 归属逻辑**——
逐条验证：两个查询在美区数据上给出完全相同的 65 个集群与 24 个歧义集群（唯一差异是
`vendor_id` 常量 4→5，Aliyun→AWS）。**Redis 一半不可移植**：国内版依赖的 config-center 数据源绑定表
在美区不存在（`luckyus_gaea` 是业务配置平台，零行提及 redis/cachecloud），继续沿用 `ozono.cache_project`
作为归属来源，`gaea` 数据只作为交叉验证证据。

#### 2.3 两处冲突按配置中心裁定，4 条映射从「猜测」变「已确认」

`luckyus-web`、`luckyus-session` 两处此前的人工猜测与配置中心结果冲突，**按配置中心为准**改判；
`luckyus-ifitax`、`luckyus-ilkm` 两条猜测被配置中心独立证实，标记确认。
`service_map.yaml` 现为 **61 行，4 行已确认（此前全部 60/60 未确认）**。

#### 2.4 部署完成 + batch 1（#100）三条判据全绿签收

08-28 20:44 exporter 重部署到 dbtools01，三族映射（63/63/85）在本地与 VM 上一致，
`service_map_up=1`，无 `multiple matches` 隐患。**08-31，batch 1（#100）观察期满，三条判据全绿签收**：
domain 非空 11/11、7 张飞书卡片逐块 @ 与服务树 role=0 精确匹配、告警形态未变。
本期 Zeus 实查佐证：**#100 本周实际触发 20 次，domain 全部非空**，是这条改造上线以来第一个完整周的生产验证。

同一天核销一处误判：判据 3（形态不变）曾一度显示失败，原因是 David 08-28 给 #100 加了个人飞书机器人
（与表达式改写无关）；新增 `--added-channel` 声明后区分「通道新增」与「通道消失」，两条路径都测过。

#### 2.5 遗留清单更正一处

08-28 复核推翻此前挂着的「补 3 条映射」待办：**horae 已由 exporter 自动解析**（CMDB 08-27 新增登记后
自动拾取），`ldasverify01/02` 属 DBA 自有工具实例，按范围规则本就不该 @ 到人——一条都不用补，
映射覆盖率按「可 @ 资格实例」算已是 100%。

**关键路径现在在 David 手上**：第 2a 批（#101 #105 #106 #124 #186 #121，6 条同族同键）已连续两次干跑
Δ0（08-28、08-31 各跑一遍结果一致），**表达式改写已就绪、待落地上线**，不构成 DBA 侧阻塞。

### 主线三：QM 2026-08 质量月报数据包（08-31，≈0.3 天）

主仓 1 次提交，+238,704（原始抓取 JSON/xlsx，不计入「净增代码」口径）。产出 8 份 xlsx（PQNC 全量、
供应商稽核、商品核销等）+ 取数说明与校验文档 + 可重跑的 `qm-2026-08/scripts/` 拉取管道，
延续 032 期「文档要面向下一步执行」的约定——原始数据与可重跑脚本分离存放，便于下月直接复用管道。

### 支线：alert-mailserver 飞书深链修复（09-02，≈0.1 天）

上期（032 遗留）「周报/月报」卡片深链上线后，08-19 提交的 `editPanel` 版本被发现是错的——
它打开 Grafana 的面板**编辑器**而非只读视图，对 Viewer 角色的同事会直接退化回看板首页，
本该收到报告的人反而最难看到它。改为 `viewPanel`（全屏只读、任意角色可用），并补了两项回归测试：
「周报/月报」行必须以展开状态发布、面板 103 必须在顶层可被 `viewPanel` 找到——防止再被误折叠一次。

---

## 三、上期（032）遗留闭环验证 —— 2026-09-03 实测

### ✅ 已闭环：batch 1（#100）三条判据全绿签收

08-31 签收，本期 Zeus 实查佐证 20 次触发、domain 100% 非空，详见 2.4。

### ✅ 已闭环：`00-工作计划_v1.0.md` 3.2b 节更正（032 #6）

08-28 c7ca771 已更正「分组 @ 大概率取并集」的错误表述为实测结论（逐块各 @ 各的）。

### 🟡 部分进展：`service_map.yaml` 核实（032 #9）

61 行中 4 行已确认（web / session / ifitax / ilkm），57 行仍待确认，非阻塞项。

### 🟡 部分进展：Argus / QM / QA 月报侧遗留（032 #17）

QM 2026-08 数据包已拉取完成（主线三），Argus / QA 两侧本期零提交，无进展。

### ❌ 仍未闭环：`EBSByteBalance%` 告警（032 #1，连续三期最高优先级）

**2026-09-03 实查全账号 CloudWatch 告警：共 36 条，EBSByteBalance% / EBSIOBalance% 仍是 0 条**，
与 031、032 两期核查结果完全一致，**三期无任何进展**。33 台 t4g.micro 同风险。

### ❌ 仍未闭环：ES 磁盘 per-node 告警（032 #2）+ 需要正确解读的 0 条

Zeus 实查：本报告周期 `luckylfe-log` ES 磁盘告警**再次 0 条触发**（连续两期 0）。
现行域级 `Average` 阈值结构上不可能提前预警这一结论未变，**「没告警」依然不等于「安全」**。

### ❌ 仍未闭环：08-24 三份研判产物提交发布（032 #3）—— 本期又新增一份同类未提交产物

`icyberdata` 存储分析 v3、`isalestradecapi` 08-24 P1 RCA **仍为未跟踪文件**，两期无进展；
本期 08-28 新出的 `isalestradecapi` P0 二次研判（同一整点批量模式，判定非故障）**同样未提交**，
未提交清单从 2 份变成 3 份。

### 其他承接项（本期无变化）

卡片长度截断风险（032 #5）、#303 抖动定性（#7）、平台侧两件事（#8）、`luckylfe-log` 保留策略/分片治理
（#11）、`luckyus_horae` 拆库排期（#12）、`icyberdata` 根因整改（#13）、风控三项（#14）、`isalescdp`
高频单行写（#15，本期第 100 号策略 20 次触发均是它）、`cyberdata` 短信轰炸/pod-cpu 抑制（#16）——详见第六节。

---

## 四、时间线

| 日期 | 主要产出 |
|---|---|
| **08-28（五）** | **DB 告警@负责人改造建仓收尾（全天，12 次提交）**：验证并采用国内 exporter 的 MySQL 归属逻辑、发现并纠正 032 期「US 无 Redis 数据源」的错误结论、`luckyus_gaea`→`luckyus_gaea_new` 切换、两处冲突按配置中心裁定 + 2 条猜测转确认、mechanize 批次验收脚本、部署到 dbtools01（20:44）并端到端验证。同日**快速研判 08-28 isalestradecapi P0**（13:32 出 RCA，判定整点批量尾巴） |
| 08-29 ~ 08-30（六日） | 无人工提交（仅自动化定时快照/数据刷新） |
| **08-31（一）** | **batch 1（#100）观察期满签收**（三条判据全绿）；**QM 2026-08 质量月报数据包**拉取完成（8 xlsx + 校验文档 + 可重跑管道） |
| **09-01（二）** | **慢 SQL L0+L1 全量审计全天（14 次提交，跨 4 个仓库）**：L0 TOP3 → 剩余 9 条 + 两份 CR → 按执行账号回溯揪出两处自身全表扫（store-ops 2,700 万行/周、效能看板 1.95 亿行/周，**当天修复**）→ 65 台五级分级定稿（两次收敛）→ L0+L1 TOP10 → 原文口径按 48 台重算定稿。同日**看板连锁修复**：`luckin-ops-dashboard` 7 处应用层修复（KeyError 清零 bug、员工姓名脱敏、周报编造数据/404 封面/SMTP 未同步三处长期静默故障、`code_version` 自证）、`luckin-store-ops-dashboard` 全表扫描修复 + 增量拼接 + 错峰、`luckin-efficiency-dashboard` 索引失效修复（压单数字曾误报 3 个数量级） |
| **09-02（三）** | **慢 SQL 18 条清单 EXPLAIN 复核收敛为 10 条有效**（剔 5 合 3 改判 2，顺带查出两处队列积压），出**可外发版 TOP10**；**alert-mailserver 飞书深链改用 `viewPanel`**（修正 08-19 的 `editPanel` 误判） |
| **09-03（四）** | 上期遗留项实测复核（Zeus + CloudWatch）；本周报整理 |

---

## 五、产物与仓库状态

### 交付物清单

| 产物 | 路径 / 位置 |
|---|---|
| L0 TOP3 / 剩余 9 条 / 按账号回溯 / 效能看板复盘 / L0+L1 TOP10 / 18→10 收敛 / 可外发版 | `reports/LKUS-slow-query-*-2026-09-0{1,2}.md`（7 份） |
| 65 台实例五级分级表 | `luckin-slow-sql-tier-map.csv` |
| 慢 SQL 分析台账 + 周度分诊工具 | `reports/slow-sql-weekly/`（`analyzed-registry.csv`、`weekly_slow_sql_triage.py`） |
| 两份索引变更申请（cdpactivity、salesorder） | `reports/变更申请_*_2026-09-01.md` |
| QM 2026-08 数据包（8 xlsx + 校验 + 可重跑管道） | `qm-2026-08/` |
| DB 告警 @负责人改造：更新的工作计划 / DEPLOY / 映射清单 | `db-alert-owner-mapping/`（`00-工作计划_v1.0.md`、`DEPLOY.md`、`data/service_map.yaml`） |
| 批次验收机械校验器（三条判据） | `db-alert-owner-mapping/check_batch_ack.py` |
| 08-28 `isalestradecapi` P0 二次研判 | `reports/2026-08-28-isalestradecapi-p0/RCA.md` ⚠️**未提交** |
| （承 032）`icyberdata` 存储分析 v3 + `isalestradecapi` 08-24 P1 RCA | ⚠️**仍未提交**（两期无进展） |

### 提交记录（代表性）

**luckin-usa-dba-ops（主仓，分支 `weekly-clean`，19 次）**
```
db3d0b4 2026-09-02  出可外发版《LKUS 慢查询优化建议 TOP 10》：只有 SQL 和建议
6791813 2026-09-02  清单从 18 条收敛到 10 条有效：剔除 5 条争用受害者，合并 3 条同动作条目
b696803 2026-09-02  待复核 4 条跑完 EXPLAIN：2 条成立、2 条推翻，另查出两处队列积压
d4557ec 2026-09-01  TOP10 改为「有优化价值」榜：产不出动作的条目直接排除
21983c3 2026-09-01  按执行账号复查 L0 慢 SQL：找出一条榜单看不见的、我们自己的
bf4549d 2026-09-01  Add L0 slow query audit report (LCNA-DBA-SQL-2026-0901)
1fbc420 2026-08-31  Add 2026-08 QM quality-report data pack (LKUS)
```

**db-alert-owner-mapping（13 次）**
```
4e38799 2026-08-31  Sign off batch 1 (#100): all three criteria green
f83884a 2026-08-28  Deployed on dbtools01 and verified end to end
cd398b5 2026-08-28  Config center wins the two conflicts, per David
ec59279 2026-08-28  Read luckyus_gaea_new, and correct numbers I got from a truncated query
1a1016b 2026-08-28  The config center does exist here: luckyus_gaea on framework01
e3a5aa2 2026-08-28  Take the domestic exporter's mapping logic for the MySQL half
c7ca771 2026-08-28  Batch 1 holds at 4/4, and the three "missing" mappings need zero work
```

**luckin-ops-dashboard（9 次实质提交，另 65 次定时快照）**
```
740db42 2026-09-01  Stop full-scanning t_order_make once an hour for one number
b6f6ca0 2026-09-01  Restore the 18:00 payload over the zeroed 18:05 one
bfd2843 2026-09-01  Fix the KeyError that zeroed the board, and stop zeros from publishing
7bb7a4a 2026-09-01  Build the weekly report from real numbers
982be65 2026-09-01  Stop publishing staff names on a public URL
```

**luckin-store-ops-dashboard（3 次实质提交，另 5 次数据刷新）**
```
40a7917 2026-09-01  Add a one-command redeploy for the collection container
41c3f38 2026-09-01  Full-rebuild when the previous payload is too old to splice onto
1da835a 2026-09-01  Stop this pipeline from full-scanning t_order every night
```

**luckin-efficiency-dashboard（3 次）**
```
4c60741 2026-09-01  Daily collector: re-query 3 days, not 180
73e49da 2026-09-01  Fix the two queries that took this dashboard off the air
```

**alert-mailserver（2 次）**
```
aae53d5 2026-09-02  Open the report panel in view mode, not the panel editor
2924c75 2026-09-02  Open the period's report panel from the Feishu card, not the dashboard
```

### 仓库同步状态

| 仓库 | 状态 |
|---|---|
| `luckin-usa-dba-ops`（主） | ✅ 已推送 |
| `db-alert-owner-mapping`（私有） | ✅ 已推送（工作树干净） |
| `luckin-ops-dashboard` | ✅ 代码已推送；⚠️ 生产容器是否已 `restart.sh` 生效待确认 |
| `luckin-store-ops-dashboard` | ✅ 代码已推送；⚠️ 采集容器未重启，自 09-01 起无新数据 |
| `luckin-efficiency-dashboard` | ✅ 已推送 |
| `alert-mailserver` | ✅ 已推送 |
| **08-24（×2）+ 08-28（×1）研判产物** | ⚠️ **均为未跟踪文件，未提交发布** |

---

## 六、遗留事项与下周待办

| # | 事项 | 来源 | 优先级 |
|---|---|---|---|
| 1 | **加 `EBSByteBalance% < 30%` 持续 15 分钟告警**。09-03 实查：全账号 36 条 CloudWatch 告警中该指标**仍是 0 条**，连续三期无进展 | 承接 030/031/032（**三期未闭环**） | **最高** |
| 2 | 🔴 **ES 磁盘告警改 per-node `Minimum` + 阈值 20 GiB**。连续两期 0 条告警**不等于安全**——域级 Average 结构上不可能提前预警 | 承接 031/032 | **最高** |
| 3 | **推动第 2a 批（#101 #105 #106 #124 #186 #121）落表达式上线**：两次干跑均 Δ0，纯属改写就绪、等 David 执行；#105/#124 是本项目首次能观察升级链路真正 @ 到人 | @负责人改造（**关键路径在 David**） | **高** |
| 4 | **三份研判产物提交发布**（08-24 icyberdata v3 + isalestradecapi P1 RCA、08-28 isalestradecapi P0 RCA）——未提交清单本期又多了一份 | 承接 032（**两期无进展且在扩大**） | **高** |
| 5 | **确认 `luckin-ops-dashboard` 生产容器已 `restart.sh` 生效**——同类漏重启上次教训是 11 天/108 份旧代码快照 | 本期 | **高** |
| 6 | **`luckin-store-ops-dashboard` 采集容器重启**：新增一键脚本已就绪，自 09-01 01:00 起已 2 天多无新数据推送 | 本期 | **高** |
| 7 | **10 条慢 SQL 清单分发给对应负责人**（张晓松等）跟进落地；`t_cyber_ug_data_fetch_task` 加复合索引后**须复跑 EXPLAIN 确认不再走 PRIMARY**（此表已在 LIMIT 陷阱上栽过一次） | 本期 | 高 |
| 8 | **两处队列积压交研发确认**：`t_cyber_ug_data_fetch_task` id=18552 卡 3 个月；`t_private_domain_user` 85 行 `user_no` 全 NULL 积压近 10 个月（与承接 015 `salespayment` 空转 17 个月同类问题） | 本期 | 中 |
| 9 | `service_map.yaml` 剩余 **57 条待人工核实**（confirmed_by 留空） | @负责人改造 | 中 |
| 10 | 卡片长度截断风险对策（分组窗口/条数上限）——**第 3 批 Redis（85 端点）上线前必须处理** | 承接 032 #5 | 高 |
| 11 | `#303` 抖动现象定性、平台侧两件事（飞书关键词拒收静默、`GeneratorURL` 明文凭据） | 承接 032 #7/#8 | 中 |
| 12 | `luckylfe-log` 保留策略复核 + 分片治理；`luckyus_horae` 拆库窗口排期 | 承接 031/032（**多期无进展**） | 高 |
| 13 | `icyberdata` 根因整改（`task_instance` 全列 `updateById`）；风控相关三项 | 承接 032 | 高 / P1 |
| 14 | `isalescdp` 高频单行写治理——本期 #100 策略 20 次触发均由它产生，是当前 DB 告警族里触发量最大的单一根因 | 承接 029/030/031/032（**五期无变化**） | 高 |
| 15 | `cyberdata` 短信轰炸治理、pod-cpu 冷启动抑制（本期【pod-cpu-兜底】P0 触发 3 次）、Argus/QA 月报侧遗留 | 承接 029~032 | 中 |

### 已闭环（本期核销）

| 事项 | 来源 | 验证方式 |
|---|---|---|
| ✅ **batch 1（#100）三条判据全绿签收** | 承接 032 关键路径 | 08-31 domain 11/11、7 张卡片逐块 @ 全一致；09-03 Zeus 实查本周 20 次触发、domain 100% 非空 |
| ✅ **`00-工作计划_v1.0.md` 3.2b 节更正** | 承接 032 #6 | 08-28 已更正「分组@取并集」的错误表述 |
| ✅ **`luckin-ops-dashboard` 三处长期静默故障修复**（周报编造数据 / 404 封面 / SMTP 未同步） | 本期 | 09-01 三次提交合并，README 已记录修复 |
| ✅ **两处自身导致的全表扫描定位并修复**（store-ops 2,700 万行/周、效能看板 1.95 亿行/周） | 本期慢 SQL 审计 | 当天合并，改为增量/复合索引，`EXPLAIN` 已复核 |

---

*本报告由 DBA 团队生成 · LCNA-DBA-2026-033 · 2026-09-03*
