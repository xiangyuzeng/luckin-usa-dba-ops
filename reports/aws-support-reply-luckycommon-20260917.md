# 给 AWS Support 的回复草稿 —— luckycommon（2026-09-17）

**本次回复的性质**：更正我方上一封第 3 点中的两处表述错误 + 提供贵方索要的证据 + 在更正后的时间线上重述实质诉求（诉求不变）。
**附件**：`exhibit-A_event11_1min_raw.csv`（1 分钟粒度原始数据）、`exhibit-B_event11_1min_chart.png`（标注图）。
**建议严重性**：维持 `High — Production system impaired`。

---

感谢核对。**贵方的观测是对的，我方上一封第 3 点的两处表述有误，在此更正。** 以下逐项说明，并附上可复算的原始数据。

## 1. 更正一：「全部节点指标均中断」是错误表述

我方上一封写的是「20:45–21:15 …… 全部 4 个数据节点与全部 3 个主节点的 CloudWatch 指标**均中断**」。这句话有两层错误：

**第一层（文字错误）**：我方内部报告的原文是「均**无**一分钟中断」，发信时漏字，变成了意思完全相反的「均中断」。贵方按字面理解并提出质疑是正确的。

**第二层（口径错误）**：即使按原意「均无中断」，该结论同样不成立。我方当时使用 **5 分钟粒度**核查，而 `uHd_i_NhTuOnijaIMvTCwA` 的第一段缺失为 **20:45–20:48（4 分钟）**，恰好落在标签为 20:45 的 5 分钟桶（覆盖 20:45:00–20:49:59）内 —— 该桶因含有 20:49 的数据点而显示为有值，**4 分钟的缺口被粒度完全掩盖**。

改用 **1 分钟粒度**重新核查后，与贵方结论一致：

| 节点 | 角色 | 20:30–22:00 UTC 缺失分钟数 |
|---|---|---|
| `uHd_i_NhTuOnijaIMvTCwA` | 专用主节点 | **21 分钟**（20:45–20:48 共 4 分钟；21:17–21:33 共 17 分钟） |
| `zip4p__KRSua3vTT-LK37A` | 专用主节点 | 0 |
| `fveZ-PKLTSus14WVO7eMLQ` | 专用主节点（当时 elected） | 0 |
| `WZNuw2r0TKWIWzUWVKboUQ` | 数据节点 | 0 |
| `5a-S3WqQSLuABRVeJ-MGBw` | 数据节点 | 0 |
| `KLaSK0NZQS-5m4jwH8uw5A` | 数据节点 | 0 |
| `ovXuEIIxTJmTs_bTiCWY-A` | 数据节点 | 0 |

**即：只有 `uHd_i` 一个节点出现指标中断，其余 6 个节点一分钟都没断。** 这与贵方「其余节点的 CPU 曲线未见中断」完全一致。

贵方给出的 `20:44–20:49`、`21:16–21:34` 两段，与我方上表是同一事实的两种写法（贵方取「最后一个正常点—第一个恢复点」，我方取「实际缺失的分钟」）。`Nodes` 亦一致：我方按 `Minimum` 统计量为 **20:47 由 7 降至 6，21:33 恢复为 7**。

## 2. 更正二：61.3% 的出处，以及它不属于 21:35

**61.3% 是真实数值，但我方把它系在了错误的时刻上。**

其确切出处为：`MasterCPUUtilization`，NodeId `uHd_i_NhTuOnijaIMvTCwA`，**统计量 `Average`，周期 `300` 秒**，时间戳标签 **20:45 UTC** 的数据桶，值 `61.3333`。

该 5 分钟桶内只有 **20:49 一分钟**有数据（20:45–20:48 缺失），因此这个「5 分钟平均」实际上等于 20:49 单分钟的平均值。同一分钟在 `Maximum`/60 秒下为 **84%**。

因此正确的表述是：**该节点在 20:49 第一次恢复上报时出现 84%（1 分钟 Maximum）/ 61.3%（5 分钟 Average）的尖峰**，而不是我方所写的「21:35 归队时尖峰 61.3%」。

贵方所述 **21:35 = 37%** 我方已复核，**完全正确**，我方数据与之逐点一致。

> 口径说明：我方前几封信中引用的节点 CPU 数值，默认使用 `Average` / 300 秒（CloudWatch 控制台默认口径）；贵方使用 `Maximum` / 60 秒。两者在缺口附近差异极大。**自本封起，我方所有数值一律标注「指标名 + 统计量 + 周期」**。同一问题也存在于我方此前提到的 09-16 06:15 事件「57.3% 尖峰」：该值为 `Average`/300s（06:30 桶，57.25%），对应 `Maximum`/60s 为 **99%**。

## 3. 更正后的事件 11 时间线（这是我方真正想提请调查的部分）

两处更正**不影响**我方的实质观察，反而使其更清晰。按 1 分钟粒度重建（原始数据见附件 A，图见附件 B）：

| 时刻 (UTC) | 观测 |
|---|---|
| 20:44 | `uHd_i` 最后一个正常数据点（9%）。`Nodes`=7 |
| **20:45–20:48** | `uHd_i` **停止上报指标（4 分钟）**。其余 6 节点无任何中断 |
| 20:47 | `Nodes` 由 7 降至 6 —— **注意：指标先断 2 分钟，节点才离开集群** |
| 20:49 | `uHd_i` 恢复上报，**84%**（Max/60s）。`MasterReachableFromNode` 转为 0 |
| **20:49–21:16** | 🔴 **该节点每一分钟都在正常上报（4–15%），但 `Nodes`=6、`MasterReachableFromNode`=0，持续 28 分钟** |
| 21:17–21:33 | `uHd_i` 再次停止上报（**17 分钟**）—— 宿主/进程重启特征 |
| 21:27–21:31 | `MasterReachableFromNode` 再次为 0 |
| 21:33 | `Nodes` 恢复为 7 |
| 21:34 / 21:35 | `uHd_i` 恢复上报：11% → **37%** |

**关键点在 20:49–21:16 这 28 分钟**：该主节点的宿主与进程均存活（在持续发布 CloudWatch 指标），但它不在集群里。**这与贵方在 09-14 为数据节点 `5a-S3WqQSLuABRVeJ-MGBw` 提供的日志所描述的形态完全同构** —— 进程未崩溃、宿主存活，因传输连接被关闭而被踢出集群。

这一点并不与贵方的观测冲突：贵方所说「其余节点 CPU 曲线未见中断」针对的是另外 6 个节点；我方所指的是 `uHd_i` 自身在 20:49–21:16 期间**有数据**这一事实。

## 4. 据此，请求的日志范围保持不变并细化

请提供 `uHd_i_NhTuOnijaIMvTCwA` 在 **2026-09-15 20:40–21:40 UTC** 的节点日志，重点回答三个已定位到分钟的问题：

1. **20:45 前后**：是什么导致该节点停止上报指标，且在 2 分钟后（20:47）被移出集群？是否与 09-14 同样出现 `ClosedChannelException`？
2. **20:49–21:16**：该节点进程存活并持续上报指标，却持续 28 分钟未能重新加入集群、`MasterReachableFromNode`=0。此期间它的日志在报什么？
3. **21:17–21:33**：这 17 分钟的硬性指标中断是否对应一次宿主/进程重启？如是，由谁发起、原因为何？

另外，该节点两次恢复上报时的 CPU 尖峰（20:49 = 84%，以及 09-16 06:30–06:32 连续 99%/98%/84%，均为 `Maximum`/60s）呈现的是**进程启动/恢复**的特征，而非稳态负载 —— 这也与「重启」判断相符，供贵方交叉验证。

## 5. 关于截图

我方以 **`get-metric-data` 的原始返回**代替控制台截图，因其可被贵方直接复算、且不受控制台默认口径影响：

- **附件 A** `exhibit-A_event11_1min_raw.csv` —— 2026-09-15 20:30–22:00 UTC，1 分钟粒度，7 个节点 CPU + `Nodes` + `MasterReachableFromNode`。**空单元格 = 该分钟无数据点**。
- **附件 B** `exhibit-B_event11_1min_chart.png` —— 上述数据的标注图。

复算命令（`MasterCPUUtilization` 为例）：

```bash
aws cloudwatch get-metric-data --region us-east-1 \
  --start-time 2026-09-15T20:30:00Z --end-time 2026-09-15T22:00:00Z \
  --scan-by TimestampAscending \
  --metric-data-queries '[{"Id":"m","MetricStat":{"Metric":{
     "Namespace":"AWS/ES","MetricName":"MasterCPUUtilization","Dimensions":[
       {"Name":"DomainName","Value":"luckycommon"},
       {"Name":"ClientId","Value":"257394478466"},
       {"Name":"NodeId","Value":"uHd_i_NhTuOnijaIMvTCwA"}]},
     "Period":60,"Stat":"Maximum"}}]'
```

## 6. 仍在等待答复的事项（未受本次更正影响）

1. **15 天内 5 次专用主节点替换的原因与时间**（3 个槽位出现 8 个 NodeId）—— 此项为本 case 当前核心。
2. 09-14 `5a-S3` 掉线中，**是什么在 12:46:53,533 关闭了该节点的出站传输通道**（`ClosedChannelException` 是机制，不是根因）。
3. 正式**撤回或更正**「t3.small 专用主节点突增算力额度不足」这一根因判定（贵方已确认 `MasterCPUCreditBalance` 为 T2 专有指标、无任何数据支持该判定）。
4. 自动快照可见性矛盾（`_cat/snapshots` 为空而 `AutomatedSnapshotFailure` 连续 14 天为 0）及三个已删除索引（`es_task_2026-09-1x`）能否恢复。
5. 恢复流量汇聚问题的推荐拓扑；服务软件版本 `R20260720` 是否含节点稳定性修复；本域推荐的专用主节点实例类型。

再次感谢贵方的细致核对 —— 它直接促使我方发现了自身的粒度口径问题。
