# 给 AWS Support 的回复草稿 —— luckycommon（2026-09-16）

**建议同时提出的处理**：请将本 case 的根因判定从「t3.small 专用主节点突增算力额度不足」**更正/撤回**，并按第 3 点继续调查。
**建议严重性**：维持 `High — Production system impaired`（掉线仍在以约每天 1.5 次的频率发生，且已出现第 5 次主节点替换）。

---

感谢详细的日志分析，第 2 点的答复很有价值。逐条回复如下。

## 1. 关于信用额度数据

感谢确认。我方同意 `MasterCPUCreditBalance` 为 T2 专有指标、t3.small 不发布 —— 我方在账户内核查 `AWS/ES` 命名空间全部 177 个指标名，确实没有任何信用额度相关指标。

据此我方理解为：**「突增算力额度不足」这一根因判定没有任何观测数据支持，是由实例类型推断得出的。** 结合贵方第 2 点已给出该次掉线的实际原因（传输层断连），我方请求**正式撤回或更正该根因判定**，以免它作为本 case 的结论留存。

作为补充，我方提供实测口径（供贵方核对，我方主动披露对自己不利的部分）：

- 被选举主节点确实会略高于 20% 基准线运行。`fveZ-PKLTSus14WVO7eMLQ` 自 09-13 20:00 接任 elected master 后，连续 52 小时运行于 20.13–23.10%（均值 21.29%）。
- 按 t3.small 标准模式最坏假设（2 vCPU / 基准 20% / 24 credit·h⁻¹ / 上限 576、起点满额）模拟：净消耗 1.54 credit/h，52 小时后余额 495.7 / 576（**86%**）。
- **从满额消耗到 0 需要连续 369 小时（15.4 天）。**
- 而本域主节点平均约 **3 天**就被替换一次（见第 3 点），**任何节点都活不到 15.4 天**，额度在物理上无法累积到耗尽。

非选举主节点全程 6–10%，持续净积累。

## 2. 关于 09-14 12:46 数据节点 `5a-S3WqQSLuABRVeJ-MGBw` 的掉线

接受该分析，与我方 CloudWatch 观测完全吻合（进程未崩溃、无 OOM、无 GC 停顿、掉线前一秒仍正常写入、指标 79 分钟全程不中断）。

有两点请求：

**2a.** 日志显示 `master_left` 发生在 12:46:54,384，距 12:46:53,533 的 `ClosedChannelException` 仅 0.85 秒，而 ping 配置为 `tried [3] times, each with maximum [30s] timeout`。三次 ping 在不到 1 秒内全部失败，表明这不是超时（链路慢），而是连接被关闭后立即失败（链路断）。请确认这一理解。

**2b.** `ClosedChannelException` 描述的是**机制**，尚未回答**根因**：**是什么在 12:46:53,533 关闭了该节点的出站传输通道？** 贵方答复中已指出这符合「该节点自身网络路径/实例层出现问题」的特征 —— 该层面完全在 AWS 托管范围内，我方无从观测。请提供该时刻该实例的网络/实例层事件记录。

## 3. 关于宿主机/底层事件记录 —— 此项现已成为本 case 的核心

理解贵方正在内部沟通。在等待期间，我方又取得了新的观测结果，请一并纳入调查：

### 3a. 故障仍在持续，且未收敛

09-15 之后 28 小时内又发生 3 次节点掉线（全部为专用主节点，全部时段集群为 GREEN、`ClusterIndexWritesBlocked`=0，因此我方未收到任何告警）：

| 起始 (UTC) | 结束 | 时长 | 最低 Nodes | `MasterReachableFromNode`=0 区间 |
|---|---|---|---|---|
| 09-15 20:45 | 21:35 | 50 min | 6 | 20:45–21:20、21:25–21:35 |
| 09-15 23:25 | 09-16 01:05 | **100 min** | 6 | 23:25–23:30 |
| 09-16 06:15 | 06:35 | 20 min | 6 | 06:15–06:20、06:25–06:35 |

累计 2026-09-07 ~ 09-16 共 **13 次**节点掉线。全部 4 个数据节点在这 3 次中指标均无中断，故均为主节点侧事件。

### 3b. 15 天内该域出现了 8 个不同的专用主节点 NodeId

`list-metrics` 对 `MasterCPUUtilization` 返回 8 个 NodeId，而本域只有 3 个专用主节点槽位。逐个拉取存活区间（2026-09-02 00:00 ~ 09-16 16:00 UTC）：

| NodeId | 首个数据点 | 末个数据点 |
|---|---|---|
| `HSC3q7p5SwO7LRIvWg2SQw` | ≤09-02 00:00 | 09-02 14:00 |
| `Uu1J7H2ZSr6nMA1Zo0p0XQ` | 09-02 15:00 | 09-05 02:00 |
| `crM3FA6WQna-TcbPyg-ZRg` | ≤09-02 00:00 | 09-05 09:00 |
| `NGfx2-02SgWO3ekOUHAy0w` | 09-05 03:00 | 09-09 11:00 |
| `fveZ-PKLTSus14WVO7eMLQ` | ≤09-02 00:00 | **09-15 23:00** |
| `zip4p__KRSua3vTT-LK37A` | 09-05 前后 | 运行中 |
| `uHd_i_NhTuOnijaIMvTCwA` | 09-09 12:39 | 运行中 |
| `iwVBdpKTSn6RTn9W_8LG1w` | **09-16 01:00** | 运行中 |

Elasticsearch 节点 ID 持久化于节点 data path，进程重启会保留同一 NodeId；NodeId 变更只可能来自节点被重建或替换。**3 个槽位出现 8 个身份，意味着 15 天内至少发生了 5 次主节点替换，平均约 3 天一次。**

**请确认：这 5 次替换是否为 AWS 侧发起的主机替换？如果是，请提供每次替换的原因与时间。** 我方未提交过任何配置变更（最后一次配置变更为 2026-06-12 EBS 扩容，已完成）。

### 3c. 请特别调取 09-15 20:45–21:35 这次的节点日志

该次事件在同一次故障中呈现了两种形态，是最接近贵方第 2 点所述机制的标本：

- **20:45–21:15**：Nodes 由 7 降至 6，但全部 4 个数据节点与全部 3 个主节点的 CloudWatch 指标**均无一分钟中断** —— 与 09-14 `5a-S3` 的「被踢出集群、宿主存活」完全同构；
- **21:15–21:30**：`uHd_i_NhTuOnijaIMvTCwA` 指标硬性中断 15 分钟 —— 宿主/进程重启；
- **21:35**：归队，Nodes 恢复 7，该节点返回时 CPU 尖峰 61.3%。

既然贵方已能为 09-14 的数据节点提供完整节点日志，请同样提供该主节点在 09-15 20:40–21:40 UTC 的日志，特别是是否同样出现 `ClosedChannelException`。

## 4. 其他仍未答复的原始问题

以下为 09-14 首次提交时的问题，尚未收到答复，请一并处理：

1. **自动快照可见性**：`GET _cat/snapshots/cs-automated-enc?v` 与 `GET _snapshot/cs-automated-enc/_all` 均返回空，而 `AutomatedSnapshotFailure` 连续 14 天为 0。请解释该矛盾，并确认本域自动快照是否可恢复。我方有三个索引（`es_task_2026-09-1x`，于 09-14 13:21–13:22 UTC 被删除）可能需要恢复。
2. **恢复流量汇聚问题**：4 个数据节点分布于 2 个 AZ，丢失 1 个数据节点会使其全部分片副本重建集中到同 AZ 的另一个节点上，该节点 29 分钟内磁盘占用由 50% 升至 84% 并导致分配停滞。请给出推荐拓扑（3 AZ / 增加数据节点 / 扩大卷）。
3. **服务软件版本**：本域停留在 `Elasticsearch_6_8_R20250625`，`R20260720` 已可更新。请确认该版本是否包含与节点稳定性或节点失联相关的修复。
4. **主节点实例类型建议**：请给出针对本域（17 索引 / 约 105–112 分片 / 7 节点）的推荐专用主节点实例类型。请注意我方不会以「突增额度耗尽」作为变更依据（见第 1 点）。

## 5. 说明

`Nodes`、`MasterReachableFromNode`、`MasterCPUUtilization` 三项指标此前未纳入我方监控采集（现正在补），因此上述 13 次掉线在发生当时我方完全无感知，均为事后翻查 CloudWatch 才发现。同时本域 `LogPublishingOptions` 为 null，我方无任何 ES 应用日志。这正是我方需要 AWS 提供托管层视角的原因。
